---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: include release specific variables
  include_vars: "{{ loop_var }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths: "../vars"
      skip: "yes"
  loop_control:
    loop_var: "loop_var"
  tags: bitbucket

- name: include release specific tasks
  include_tasks: "{{ loop_var }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths: "."
      skip: "yes"
  loop_control:
    loop_var: "loop_var"
  tags: bitbucket

- name: prepare directories
  file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
    state: "{{ item.state | default('directory') }}"
  with_items:
    - { dest: "/etc/init.d" }
    - { dest: "/etc/systemd/system" }
    - { dest: "/opt/atlassian/bitbucket", owner: "daemon", group: "daemon" }
    - { dest: "/var/atlassian/application-data/bitbucket", owner: "daemon", group: "daemon" }
  tags: bitbucket

- name: unarchive package
  shell: |
    cd /opt/atlassian/bitbucket
    curl -skL {{ bitbucket_download_url }} | tar zxf - --strip-components 1
    cd /opt/atlassian/bitbucket/app/WEB-INF/lib
    rm -rf *mysql*.jar
    curl -skL {{ mysql_jdbc_download_url }} | tar zxf - --strip-components 1 --wildcards "mysql*/*mysql*.jar"
    cd /opt/atlassian/bitbucket/app/WEB-INF/lib
    rm -rf *postgresql*.jar
    curl -skL {{ postgresql_jdbc_download_url }} -O
    chown -Rf daemon:daemon /opt/atlassian/bitbucket
  args:
    creates: "/opt/atlassian/bitbucket/bin/start-bitbucket.sh"
  tags: bitbucket

- name: copy templates
  template:
    src: "./templates{{ item.dest }}.j2"
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  with_items:
    - { dest: "/etc/init.d/bitbucket", mode: "0755" }
    - { dest: "/etc/systemd/system/bitbucket.service" }
    - { dest: "/opt/atlassian/bitbucket/bin/_start-webapp.sh", owner: "daemon", group: "daemon", mode: "0755" }
    - { dest: "/opt/atlassian/bitbucket/bin/set-bitbucket-home.sh", owner: "daemon", group: "daemon", mode: "0755" }
    - { dest: "/opt/atlassian/bitbucket/bin/set-bitbucket-user.sh", owner: "daemon", group: "daemon", mode: "0755" }
    - { dest: "/opt/atlassian/bitbucket/bin/set-jre-home.sh", owner: "daemon", group: "daemon", mode: "0755" }
    - { dest: "/opt/atlassian/bitbucket/bin/start-bitbucket.sh", owner: "daemon", group: "daemon", mode: "0755" }
    - { dest: "/opt/atlassian/bitbucket/bin/stop-bitbucket.sh", owner: "daemon", group: "daemon", mode: "0755" }
  register: template_result
  tags: bitbucket

- name: systemctl daemon-reload
  systemd:
    name: bitbucket
    daemon_reload: yes
  when:
    - ansible_service_mgr == 'systemd'
    - template_result is changed
  changed_when: false
  ignore_errors: yes
  tags: bitbucket

- name: enable service
  service:
    name: bitbucket
    enabled: yes
  changed_when: false
  ignore_errors: yes
  tags: bitbucket
